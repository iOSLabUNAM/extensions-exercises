import Foundation

extension Int {
    /// Add here the new Enum with the **Even** and **Odd** cases
    
    
    /// Add here the property of the new Enum type to correctly report whether an integer is even or odd
}
